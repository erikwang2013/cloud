name: cloud_platform
description: "CloudPlatform - Multi-platform cloud management panel"
publish_to: 'none'
version: 3.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  cupertino_icons: ^1.0.8
  dio: ^5.4.0
  flutter_secure_storage: ^9.2.0
  provider: ^6.1.0
  go_router: ^14.0.0
  window_manager: ^0.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
