# API 自动化测试报告（2026-08-26）

## 环境
- service: http://127.0.0.1:8787（6 workers，`php start.php start -d`）
- admin 原服务(8788): B1 修复后正常启动（EncryptionBootstrap.php 与 config/encryption.php 已加 base64_decode），直接测试
- admin 测试目标: http://127.0.0.1:8788（当前由 /tmp/cp-admin-ui 副本启动，含 B1 修复；仓库代码同源）
- PHP 8.3.7 / webman 5.2.2 / MySQL 3306（cloud_platform）/ Redis 6379
- 测试数据: `apitest{ts}@test.local`（service 用户）、`apitestadmin`（admin 临时账号，报告后保留/可删）
- 鉴权: register/login 需点击验证码 → 白盒读取 Redis `poster:captcha:*` 目标坐标求解

## 结果统计
| 维度 | 数量 |
| --- | --- |
| 总断言 | 233 |
| 通过 | 221 |
| 失败 | 12 |
| 通过率 | 94.8% |

## 失败明细
- `GET /api/products/{id}` [public] status=500 期望=2xx/4xx 备注: 
- `GET /api/domain/check/{domain}/{tld}` [public] status=500 期望=2xx/4xx 备注: 
- `GET /api/domain/tlds` [public] status=500 期望=2xx/4xx 备注: 
- `GET /api/help/{slug}` [public] status=500 期望=2xx/4xx 备注: 
- `POST /api/payments/webhook/stripe` [public] status=500 期望=2xx/4xx 备注: 
- `POST /api/auth/register(加密)` [auth-flow] status=422 期望=0 备注: code=422 msg=Email or phone and password are required —— EncryptionMiddleware 将字段写入 $request 动态属性，controller 用 all()/input() 读不到 → 422（应用缺陷）
- `POST /api/auth/register(明文)` [auth-flow] status=500 期望=200 备注: HTTP 500 —— AuthService::register 用 Hash::make，容器无 hash 绑定 → 500（应用缺陷）
- `POST /app/admin/account/login` [admin] status=404 期望=200 备注: 登录路由未注册（该构建仅 4 个路由）
- `/app/admin/user/index` [admin] status=404 期望=200 备注: 路由未注册（该构建缺 CRUD 路由）
- `/app/admin/order/index` [admin] status=404 期望=200 备注: 路由未注册（该构建缺 CRUD 路由）
- `/app/admin/product/index` [admin] status=404 期望=200 备注: 路由未注册（该构建缺 CRUD 路由）
- `/app/admin/config/index` [admin] status=404 期望=200 备注: 路由未注册（该构建缺 CRUD 路由）

## 限制说明
- Stripe 支付为外部服务：`POST /api/orders/{id}/pay` 仅验证错误处理路径（4xx/5xx），无法完成真实支付
- service 的 `/admin/api/*` 端点：普通用户 token 应 401/403（AdminRole/RBAC），管理员 token 需 service 侧管理员账号（本环境未配置）
- admin 主密钥问题（B1）已修复并复测：8788 正常启动，captcha/dashboard/dict 可用；login 与 CRUD 仍 404（路由未注册，属构建缺陷非本次修复范围）
- admin 构建缺陷：config/route.php 仅注册 captcha/dict/dashboard/export 4 个路由，login 与全部 CRUD 走 404 fallback → 登录与管理列表接口无法测试
- 注册/登录（service）两条路径均不可用：加密路径字段丢失（中间件缺陷），明文路径 Hash 容器绑定缺失（500）→ 依赖用户 token 的鉴权扫描（带 token 用例）整体跳过
- 公开端点 500：products/{id}、help/{slug}、stripe webhook 均因 ModelNotFoundException 未捕获返回 500（应 404）；domain/tlds 因 DB 缺 status 列报 SQL 错（表结构漂移）
- 限流：默认 60 req/60s per IP，测试已按 1s 间隔规避，个别瞬时 429 属预期